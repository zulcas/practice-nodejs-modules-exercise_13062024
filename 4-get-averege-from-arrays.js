// importa el módulo "utils.js"
const utils = require('./mis-utilidades/utils.js')

const array1 = [1, 2, 3, 4, 5];
const array2 = [10, 20, 30, 40, 50];



console.log("La media del primer array es: ", utils.getAvgFromArray(array1));
console.log("La media del segundo array es: ", utils.getAvgFromArray(array2));
